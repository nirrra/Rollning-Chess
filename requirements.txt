# Install Rolling Chess itself and its runtime dependencies from pyproject.toml.
-e .

# Test dependencies
httpx>=0.27
pytest>=8.0

# Optional AI training dependency. Install with:
# python -m pip install -e ".[ai]"
# torch>=2.2
